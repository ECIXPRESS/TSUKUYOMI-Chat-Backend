package edu.dosw.domain.model;

public class PhotoMessage extends ConversationMessage{
    protected PhotoMessage(String conversationId, String text, String author) {
        super(conversationId, text, author);
    }
}
