package edu.dosw.domain.model;

public class Regular extends ConversationMessage{

    public Regular(String conversationId, String text, String authorId) {
        super(conversationId, text, authorId);
    }
}
