spring.application.name=Chat

# Server Configuration
server.port=8080

//tengomisdudasdeesepuerto8080

# MongoDB Configuration (mantener igual)
spring.data.mongodb.uri=mongodb+srv://manuelguarnizog_db_user:slmEouXGl5LaTDbg@clustereciexpress.m7ljcsf.mongodb.net/Chat-db?retryWrites=true&w=majority&appName=ClusterECIEXPRESS

# Redis Configuration (usa variables de entorno)
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:}
spring.data.redis.ssl=${SPRING_DATA_REDIS_SSL:false}
spring.data.redis.database=0
spring.data.redis.timeout=5000ms

# API Gateway Configuration
api-gateway.url=${API_GATEWAY_URL:http://localhost:8081}

# JWT Configuration
jwt.secret=${JWT_SECRET:defaultSecretForDevelopment}

# Server Timeouts
spring.mvc.async.request-timeout=30000

# Redis Listener Configuration
spring.data.redis.listener.enabled=true

# WebSocket Configuration
spring.websocket.allowed-origins=*

# Logging Configuration
logging.level.root=INFO
logging.level.edu. dosw=DEBUG
logging.level.org.springframework.data.redis=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.springframework.web.socket=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n